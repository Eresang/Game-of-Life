program GoL;

uses
  Forms,
  DisplayForm in 'DisplayForm.pas' {Display},
  Game_of_Life in 'Game_of_Life.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.Title := 'Game of Life';
  Application.CreateForm(TDisplay, Display);
  Application.Run;
end.
